---
const shopName = 'Sweet Flower Shop';
const tagline = 'Свежие цветы с любовью';

const features = [
  { icon: '🌸', text: 'Свежие букеты каждый день' },
  { icon: '🚚', text: 'Доставка по городу' },
  { icon: '💐', text: 'Оформление на любой случай' },
];
---

<!-- TODO: you can start here, this is for example -->
 
<div class="mx-auto px-4 py-12">
  <h1 class="text-5xl font-bold text-gray-600 mb-4">
    {shopName}
  </h1>

  <p class="text-xl text-gray-600 mb-8">{tagline}</p>

  <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
    {
      features.map((feature) => (
        <div class="bg-white p-6 rounded-lg shadow-md hover:shadow-lg transition">
          <div class="text-4xl mb-3">{feature.icon}</div>
          <p class="text-gray-700">{feature.text}</p>
        </div>
      ))
    }
  </div>
</div>
